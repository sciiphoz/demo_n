<Window x:Class="Demo2.Windows.InfoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Demo2.Windows"
        mc:Ignorable="d"
        Title="InfoWindow" Height="450" Width="800"
        WindowState="Maximized">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"></RowDefinition>
            <RowDefinition Height="5*"></RowDefinition>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button x:Name="LogoutButton" Padding="10 5" Margin="10" Click="LogoutButton_Click">Выйти</Button>
            <Button x:Name="ProductButton" Padding="10 5" Margin="10" Click="ProductButton_Click">Товар</Button>
            <Button x:Name="OrderButton" Padding="10 5" Margin="10" Click="OrderButton_Click">Заказы</Button>
        </StackPanel>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
            <Image x:Name="LogoImage" Height="65" Margin="10 0"></Image>

            <StackPanel Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Center">
                <TextBlock x:Name="UserName" Margin="5" FontSize="18"></TextBlock>
                <TextBlock x:Name="UserLogin" Margin="5" FontSize="10"></TextBlock>
                <TextBlock x:Name="UserRole" Margin="5" FontSize="10"></TextBlock>
            </StackPanel>
        </StackPanel>

        <Frame x:Name="MainFrame" Grid.Row="1" NavigationUIVisibility="Hidden"></Frame>
    </Grid>
</Window>

<Page x:Class="Demo3.Pages.ProductPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Demo3.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProductPage">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="5*"></RowDefinition>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" >
            <TextBox x:Name="SearchTB" TextChanged="SearchTB_TextChanged" Margin="10" Width="100"></TextBox>
            <ComboBox x:Name="SortCB" SelectionChanged="SortCB_SelectionChanged" Margin="10" Width="75">
                <ComboBoxItem>По умолчанию</ComboBoxItem>
                <ComboBoxItem>По цене (убыв.)</ComboBoxItem>
                <ComboBoxItem>По цене (возр.)</ComboBoxItem>
            </ComboBox>
            <ComboBox x:Name="FilterCB" SelectionChanged="FilterCB_SelectionChanged" Margin="10" Width="75"></ComboBox>

            <Button x:Name="ResetButton" Padding="10 5" Click="ResetButton_Click" Margin="10">По умолчанию</Button>
            <Button x:Name="AddButton" Padding="10 5" Click="AddButton_Click" Margin="10">Добавить</Button>
            <Button x:Name="DeleteButton" Padding="10 5" Click="DeleteButton_Click" Margin="10">Удалить</Button>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ListView x:Name="MainListView" ItemsSource="{Binding}" MouseDoubleClick="MainListView_MouseDoubleClick">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border x:Name="ItemBorder">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition></ColumnDefinition>
                                    <ColumnDefinition></ColumnDefinition>
                                    <ColumnDefinition></ColumnDefinition>
                                </Grid.ColumnDefinitions>

                                <Image Grid.Column="0" Height="150" Margin="10" Source="{Binding FullImagePath}"></Image>

                                <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Name}"></TextBlock>
                                        <TextBlock Text="|"></TextBlock>
                                        <TextBlock Text="{Binding Category.Name}"></TextBlock>
                                    </StackPanel>
                                    
                                    <TextBlock Text="{Binding Desc}"></TextBlock>
                                    <TextBlock Text="{Binding Manufacturer.Name}"></TextBlock>
                                    <TextBlock Text="{Binding Supplier.Name}"></TextBlock>

                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Price}" Margin="10">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding HasDiscount}" Value="True">
                                                            <Setter Property="TextDecorations" Value="Strikethrough"></Setter>
                                                            <Setter Property="Foreground" Value="Red"></Setter>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>

                                        <TextBlock Text="{Binding FinalPrice}">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Hidden"></Setter>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding HasDiscount}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible"></Setter>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>

                                    <TextBlock Text="{Binding Measurement.Name}"></TextBlock>
                                    <TextBlock Text="{Binding Quantity}"></TextBlock>
                                </StackPanel>

                                <TextBlock Grid.Column="2" HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding Discount}"></TextBlock>
                            </Grid>
                        </Border>

                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding BigDiscount}" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="Orange"></Setter>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding OutOfStock}" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="Cyan"></Setter>
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ScrollViewer>
    </Grid>
</Page>
